"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildSitemapXml = void 0;
const withXMLTemplate_1 = require("./withXMLTemplate");
exports.buildSitemapXml = (fields) => {
    const content = fields.reduce((prev, curr) => {
        let field = '';
        // Iterate all object keys and key value pair to field-set
        for (const key of Object.keys(curr)) {
            if (curr[key]) {
                field += `<${key}>${curr[key]}</${key}>`;
            }
        }
        // Append previous value and return
        return `${prev}<url>${field}</url>\n`;
    }, '');
    return withXMLTemplate_1.withXMLTemplate(content);
};
